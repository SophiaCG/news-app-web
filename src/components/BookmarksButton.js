import React, { useState } from "react";
import { FaRegBookmark, FaBookmark } from "react-icons/fa";

const BookmarksButton = ({}) => {
  return (
    <div id="bookmark-button-container">
      <FaBookmark id="icon-button" />
    </div>
  );
};

export default BookmarksButton;
